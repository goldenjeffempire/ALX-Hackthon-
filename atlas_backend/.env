# Django Settings
SECRET_KEY=django-insecure-@8x(z4web&0307=)&5oik@^cc_h72vb%wfw4^w565uw9p!29w-
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database Settings (provided by Replit)
PGDATABASE=${PGDATABASE}
PGUSER=${PGUSER}
PGPASSWORD=${PGPASSWORD}
PGHOST=${PGHOST}
PGPORT=${PGPORT}

# CORS Settings
CORS_ALLOWED_ORIGINS=DEBUG

# Email Settings (For development, using console backend)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@atlas-booking.com
